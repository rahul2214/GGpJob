
# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.
# GGpJob

## Initial Admin Setup
To create the initial administrator account (veltria.in@gmail.com), please visit the following URL once the application is running:
`[YOUR_APP_URL]/api/init-admin`

After initialization, you can access the admin panel at `/admin/login`.
